# Claude Code harness state (not part of the project)
.claude/

# Superseded prototype — ported into src/ (see README). Kept on disk for
# reference but not tracked, to avoid two copies of the game logic diverging.
/codebreaker.html
/index.html

# Dependencies
node_modules/

# Build output
dist/
www/
build/*.exe
build/*.dmg
build/*.AppImage

# Generated / vendored at install time (see scripts/prepare-vendor.js, build-word-db.js)
src/vendor/
src/data/words.sqlite

# Private word list — never commit real/licensed word lists (see README "Word list")
data/words-source.json

# Mobile native projects (generated by `npx cap add ios|android`)
mobile/ios/
mobile/android/

# Tauri build artifacts
src-tauri/target/
src-tauri/icons/

# Environment secrets
.env.local
.env.*.local

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/*
!.vscode/extensions.json

# Test coverage
coverage/
